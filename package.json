{
  "name": "calculo-preco",
  "private": true,
  "version": "0.1.0",
  "description": "Sistema de precificação com cálculo tributário para MEI / Simples Nacional / Reforma Tributária — 100% front-end (Angular + Firebase), sem backend próprio",
  "workspaces": [
    "packages/engine",
    "apps/web"
  ],
  "scripts": {
    "test:engine": "npm run test --workspace=packages/engine",
    "build:engine": "npm run build --workspace=packages/engine",
    "dev:web": "npm run build:engine && npm run start --workspace=apps/web",
    "build:web": "npm run build:engine && npm run build --workspace=apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
